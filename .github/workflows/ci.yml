name: "CI (Integração Continua): Main Workflows"

on:
  push:
    branches: 
      -main

jobs:
    exemplo:
       runs-on: ubuntu-latest
       steps:
        - uses: actions/checkout@v4
          
        - name: Run a one-line script    
          run: echo Hello, world!